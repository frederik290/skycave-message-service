service: 
  name: skycave-message-service

plugins:
  - serverless-bundle
  - serverless-pseudo-parameters

provider:
  name: aws
  runtime: nodejs12.x
  memorySize: 256
  stage: ${opt:stage, 'dev'}
  region: eu-west-1

functions:
  getInfo:
    handler: ./src/handlers/info.handler
    events:
      - http:
          method: GET
          path: /info