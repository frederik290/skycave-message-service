 MessageTableIAM:
  Effect: Allow
  Action:
    - dynamodb:GetItem
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:UpdateItem
    - dynamodb:Query
  Resource:
    - ${self:custom.MessageTable.arn}
